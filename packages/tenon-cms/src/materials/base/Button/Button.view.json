{
  "el": "a-button",
  "props": {
    "<BINDING>": "buttonProps",
    "onClick": "{{() => this.executeEvents$('onClick')}}"
  },
  "children": ["{{this.buttonText.value}}", "{{this.count.value}}"]
}
